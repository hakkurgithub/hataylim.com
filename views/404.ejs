<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>404 - Sayfa Bulunamadı | Hataylım Gazetesi</title>
    
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    
    <style>
        :root {
            --hataylim-red: #dc3545;
            --hataylim-dark-red: #b71c1c;
        }
        
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, var(--hataylim-red), var(--hataylim-dark-red));
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        
        .error-container {
            text-align: center;
            color: white;
        }
        
        .error-code {
            font-size: 10rem;
            font-weight: bold;
            line-height: 1;
            text-shadow: 3px 3px 6px rgba(0,0,0,0.3);
        }
        
        .error-message {
            font-size: 2rem;
            margin: 20px 0;
        }
        
        .error-description {
            font-size: 1.2rem;
            margin-bottom: 30px;
            opacity: 0.9;
        }
        
        .btn-home {
            background: white;
            color: var(--hataylim-red);
            padding: 15px 40px;
            font-size: 1.1rem;
            border: none;
            border-radius: 50px;
            text-decoration: none;
            display: inline-block;
            transition: all 0.3s;
        }
        
        .btn-home:hover {
            background: #f8f9fa;
            color: var(--hataylim-dark-red);
            transform: translateY(-3px);
            box-shadow: 0 5px 15px rgba(0,0,0,0.3);
        }
    </style>
</head>
<body>
    <div class="error-container">
        <i class="fas fa-newspaper" style="font-size: 5rem; margin-bottom: 20px;"></i>
        <div class="error-code">404</div>
        <div class="error-message">Sayfa Bulunamadı</div>
        <p class="error-description">
            Aradığınız sayfa kaldırılmış veya taşınmış olabilir.
        </p>
        <a href="/" class="btn-home">
            <i class="fas fa-home"></i> Ana Sayfaya Dön
        </a>
    </div>
</body>
</html>
